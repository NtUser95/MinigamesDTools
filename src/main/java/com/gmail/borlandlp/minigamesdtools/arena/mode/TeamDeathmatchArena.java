package com.gmail.borlandlp.minigamesdtools.arena.mode;

public class TeamDeathmatchArena
{
    /*
     * if return false -> round must be ended

    public boolean checkRoundCustomConditions()
    {
        if(this.getTimeLeft() <= 0) {
            return false;
        } else if(this.getAmountRemainSurvivedTeams() <= 1) {
            return false;
        }

        return true;
    }

    public boolean checkGameCustomConditions()
    {
        if(this.getCurrentRound() > this.getMaxRounds()) {
            return false;
        } else {
            return true;
        }
    }
*/
}
